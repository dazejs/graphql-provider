type Query {
    findAllBooks: [Book]!
    findBookById(id: Int!): Book
}

type Mutation {
    createBook(name: String!, pageCount: Int, price: Float): Book!
    updateBook(id: Int!, name: String!): Book!
}
