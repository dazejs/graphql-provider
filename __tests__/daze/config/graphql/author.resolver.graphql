type Query {
    findAllAuthors: [Author]!
    findAuthorById(id: Int!): Author
}

type Mutation {
    createAuthor(name: String!, birth: Date, gender: Gender): Author!
    updateAuthorBooks(id: Int!, bookIds: [Int]!): Author!
}
